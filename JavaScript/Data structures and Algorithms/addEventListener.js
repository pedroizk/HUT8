// the addEventListener function is a method of the event target

    const a = EventTarget();

// That means that the this function needs a target object to work,


    a.addEventListener(type, function);

// there is a lot of event types

const mouseEventTypes = ['click', 'contextmenu', 'dbclick', 'DOMActivate', 'DOMMouseScroll', 'mousedown',
    'mouseenter', 'mouseleave' , 'mousemove', 'mouseout'
]
